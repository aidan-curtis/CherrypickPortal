import Tagged from './Tagged';

export const TaggedConfig = {
    settings: {
        layout: {
            config: {}
        }
    },
    routes  : [
        {
            path     : '/apps/dashboards/tagged',
            component: Tagged
        }
    ]
};
